# Bun only inlines BUN_PUBLIC_* vars into the browser bundle.
# Point at the deployed API Worker (Express + Better Auth) base URL.
# Cloudflare Static Assets values are fixed at build time.
BUN_PUBLIC_AUTH_URL=http://localhost:8000
